== Suppressed Messages ==

# IG Publisher suppressed-messages file (new format; header line above required).
# Suppresses WARNINGS, hints and broken links only — the publisher cannot suppress
# an error, and lists every non-blank line here (comments included) with a use
# count on qa.html. Keep it minimal; document every entry.
#
# Empty on purpose. The two patterns that used to live here hid every unresolved
# `http://thespierproject.org/fhir/Questionnaire/*` canonical; since #473 the
# Questionnaires are IG artifacts (input/resources/questionnaires, a symlink to
# ig/input/resources/questionnaires/, named in sushi-config.yaml) and those canonicals resolve.
