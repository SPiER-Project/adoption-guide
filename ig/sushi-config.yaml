# SPiER FHIR Implementation Guide — Sushi configuration
#
# This IG compiles via fsh-sushi (https://fshschool.org/docs/sushi/) and is
# rendered by the HL7 IG Publisher. The output is a hostable FHIR IG site
# that complements the React companion app.
#
# Status: ci-build / draft. Not yet balloted or formally published.

id: thespierproject.fhir
# Canonical root matches the existing FHIR-Resources artifacts (Questionnaire
# url = http://thespierproject.org/fhir/Questionnaire/...) and the observationMappers code
# systems. Keep the IG and runtime data in the same namespace.
canonical: http://thespierproject.org/fhir
name: SPiER
title: The SPiER Project
status: draft
version: 0.1.0
fhirVersion: 4.0.1
copyrightYear: 2026+
releaseLabel: ci-build

description: >-
  A FHIR-native reference implementation of the suicide-safer care pathway,
  developed in alignment with the Zero Suicide framework. SPiER makes each
  layer of suicide prevention machine-actionable across three steps: Capture
  (validated instruments as FHIR Questionnaires), Translate (an
  instrument-agnostic suicide-risk concept that every tool maps into), and Act
  (PlanDefinitions and ActivityDefinitions that turn the already-settled
  response protocol into executable recommendations) — modeling the eight
  technical stages from Flag Risk through Measure and Share. The goal is to
  give EHR vendors and health systems lift-able artifacts that demonstrate
  what suicide-prevention pathway conformance looks like in practice.

license: CC0-1.0

publisher:
  name: SPiER
  url: https://htdhealth.com
  email: brad.thorson@htdhealth.com

jurisdiction: urn:iso:std:iso:3166#US "United States of America"

# US Core dependency anchors patient demographics and standard observations
# to the US realm. Add additional dependencies (e.g., SDC) as needed.
dependencies:
  hl7.fhir.us.core: 6.1.0
  # SDC: calculatedExpression (score totals) + observationExtract (declared
  # extraction contract) extensions used by the screening Questionnaires.
  hl7.fhir.uv.sdc: 3.0.0
  # CRMI and CQF Common are BOTH consequences of compiling the measure CQL, not
  # choices — nothing in the FSH references either by hand. Once a Measure has a
  # `library` pointing at compiled CQL, the publisher infers the measure's data
  # requirements, stamps a `crmi-effectiveDataRequirements` extension on the
  # Measure and contains the inferred Library inside it. Without these two the
  # publisher generates content it cannot then validate: 21 errors across the
  # seven Measures, three each, for an undefined extension and an unresolvable
  # FHIRHelpers canonical. 2.0.0 rather than 1.0.0 because its own dependency
  # pins (hl7.terminology.r4 7.x, uv.extensions.r4 5.3.0) match what this IG
  # already loads. Both are R4.
  hl7.fhir.uv.crmi: 2.0.0
  fhir.cqf.common: 4.0.1

# IG Publisher parameters
parameters:
  show-inherited-invariants: false
  apply-contact: true
  apply-jurisdiction: true
  apply-publisher: true
  # FHIR Mapping Language sources. SUSHI does not compile .fml — the IG
  # Publisher does, but only for folders it is told to scan, and the default
  # scan of input/resources does not recurse into subfolders. Naming the
  # subfolder explicitly is what makes ig/input/resources/maps/*.fml become
  # published StructureMaps (issues #92 / #229).
  #
  # The Questionnaires are the Capture layer's central artifact and live OUTSIDE
  # ig/, in FHIR-Resources/<tool>/ at the repo root, because the app imports the
  # same JSON at runtime and "one canonical URL, one definition" forbids a second
  # copy. `input/resources/questionnaires` is a SYMLINK to that tree (tracked in
  # git), and naming its tool folders here is what publishes them: SUSHI lists
  # every JSON it finds in the ImplementationGuide resource and the publisher
  # renders each as Questionnaire-<id>.html, so ActivityDefinition.relatedArtifact
  # and QuestionnaireResponse.questionnaire finally resolve (#473).
  #
  # ⚠️ A symlink, not `../FHIR-Resources`: the publisher refuses any
  # path-resource that escapes the IG root ("Computed path does not start with
  # first element"), and the alternative — moving the JSON under ig/ — would split
  # every per-tool folder across two trees and touch ~30 consumers of the
  # FHIR-Resources path.
  #
  # ⚠️ One entry PER TOOL FOLDER, never the recursive `questionnaires/*` form.
  # The publisher (unlike SUSHI) tries to load every file it finds, and the
  # tools' `references/` subfolders hold PDFs, DOCX and XLSX: the first CI run
  # with `/*` logged 34 "Error loading … as Turtle" lines, spilled their bytes
  # into publisher.log, and grep then refused to read the QA counts as text
  # (#512). A tool folder's top level holds only its JSON and a README, which
  # the publisher skips. A folder missing from this list is a Questionnaire the
  # IG silently does not publish — `check-ig-narrative.mjs` fails on a resource
  # JSON sitting one level below a listed directory that no entry reaches, so
  # add the new tool's line here when you add its folder. The two CarePlan
  # templates and the ASQ yes/no ValueSet that sit beside the Questionnaires
  # come along as examples, which is correct: they are FHIR resources the app
  # ships.
  path-resource:
    - input/resources/maps
    - input/resources/questionnaires/ASQ
    - input/resources/questionnaires/BSSA
    - input/resources/questionnaires/C-SSRS
    - input/resources/questionnaires/CAMS
    - input/resources/questionnaires/CRP
    - input/resources/questionnaires/PHQ-9
    - input/resources/questionnaires/PSS-3
    - input/resources/questionnaires/PSS-Full
    - input/resources/questionnaires/SAFE-T
    - input/resources/questionnaires/SBQ-R
    - input/resources/questionnaires/Stanley-Brown
  # THE CQL LOADER'S ACTIVATION SWITCH. The IG-parameters CodeSystem defines
  # path-binary as files "only used in the context of the CQL loader" — without
  # this line the publisher walks past input/cql in total silence, translating
  # nothing and saying nothing. #201 hit exactly that, read the silent log as
  # proof that the publisher cannot translate CQL at all, and moved the measure
  # CQL out of the build; #212 re-tested and found the capability had been there
  # the whole time. With it set the log reads "Translating CQL source in folder
  # …/input/cql", and the first compile immediately failed on five real defects.
  path-binary:
    - input/cql
  # ⚠️ TEMPORARY, and built to be deleted. The ASQ Questionnaire binds its panel,
  # its seven items and the two answer codes on the recency item to LOINC codes
  # published in LOINC 2.83 (adopted 2026-09-08; verified against Regenstrief —
  # FHIR-Resources/ASQ/README.md). tx.fhir.org still serves 2.82, so the publisher
  # reports ten "Unknown code … version '2.82'" ERRORS, and an error cannot be
  # suppressed — ignoreWarnings.txt covers warnings, hints and broken links only
  # (tried; qa.html said so). This is the same edition lag `check:codings`
  # tolerates through PENDING_TX and docs/scheduled-checks-triage.md § Cause 1b
  # records as expected, so validation of this ONE resource is switched off in
  # the publisher rather than keeping the Capture layer's flagship instrument out
  # of the guide. What still checks it: validate-fhir.mjs (structure, every PR),
  # the terminology nightly (codes, with the lag recorded) and the loinc-audit
  # against Regenstrief. Delete this parameter together with the PENDING_TX
  # lines and the triage paragraph once tx.fhir.org serves 2.83 — the IG
  # parameters CodeSystem is blunt that suppressing validation is generally a
  # bad idea, and it is right.
  no-validate:
    - Questionnaire/ASQ-Screening-Tool

# Artifacts-page groups. GENERATED between the two markers below by
# `node scripts/build-ig-groups.mjs` from one rule per FSH file / tool folder /
# .fml map (the RULES table in that script); `--check` fails in ig.yml when the
# block is stale or a source has no rule. The eight groups and why the page is
# grouped by purpose rather than by resource type: docs/plans/ig-cleanup-audit-2026-09-16.md §4.
# The FML StructureMaps sit under `resources:` rather than `groups:` because SUSHI
# never loads .fml and rejects a group member it did not load.
# ── BEGIN generated groups — node scripts/build-ig-groups.mjs; do not edit by hand ──
groups:
  instruments:
    name: "Screening and assessment instruments (Capture)"
    description: "Each validated instrument as SPiER publishes it: the Questionnaire itself, the ActivityDefinition that administers it, the profile its result lands in, the answer vocabularies, and one worked QuestionnaireResponse and Observation per form."
    resources:
      - Questionnaire/ASQ-Screening-Tool
      - Questionnaire/BSSA
      - Questionnaire/C-SSRS-Full-Lifetime-Recent
      - Questionnaire/C-SSRS-Pediatric
      - Questionnaire/C-SSRS-Screener
      - Questionnaire/C-SSRS-Since-Last-Contact
      - Questionnaire/CAMS-SSF5-OutcomeDisposition
      - Questionnaire/CAMS-SSF5-SectionA
      - Questionnaire/CAMS-SSF5-SectionB
      - Questionnaire/CAMS-Stabilization-Plan
      - Questionnaire/CAMS-Therapeutic-Worksheet
      - Questionnaire/PHQ-9
      - Questionnaire/PSS-3
      - Questionnaire/PSS-Full
      - Questionnaire/SAFE-T
      - Questionnaire/SBQ-R
      - ActivityDefinition/AdministerASQ
      - ActivityDefinition/AdministerBSSA
      - ActivityDefinition/AdministerCAMSInterimSession
      - ActivityDefinition/AdministerCAMSOutcomeDisposition
      - ActivityDefinition/AdministerCAMSSectionA
      - ActivityDefinition/AdministerCAMSSectionB
      - ActivityDefinition/AdministerCAMSStabilizationPlan
      - ActivityDefinition/AdministerCAMSTherapeuticWorksheet
      - ActivityDefinition/AdministerCSSRSFull
      - ActivityDefinition/AdministerCSSRSPediatric
      - ActivityDefinition/AdministerCSSRSScreener
      - ActivityDefinition/AdministerCSSRSSinceLastContact
      - ActivityDefinition/AdministerPHQ9
      - ActivityDefinition/AdministerPSS3
      - ActivityDefinition/AdministerPSSFull
      - ActivityDefinition/AdministerSAFET
      - ActivityDefinition/AdministerSBQR
      - StructureDefinition/spier-asq-result
      - StructureDefinition/spier-bssa-disposition-result
      - StructureDefinition/spier-cams-outcome-disposition
      - StructureDefinition/spier-cams-ssf-vital
      - StructureDefinition/spier-cams-stabilization-plan
      - StructureDefinition/spier-cams-suicide-driver
      - StructureDefinition/spier-cams-therapeutic-worksheet
      - StructureDefinition/spier-cssrs-risk-level
      - StructureDefinition/spier-phq9-item9
      - StructureDefinition/spier-phq9-total-score
      - StructureDefinition/spier-pss-full-risk-level
      - StructureDefinition/spier-pss3-result
      - StructureDefinition/spier-safet-risk-level
      - StructureDefinition/spier-sbqr-total-score
      - CodeSystem/asq-age-group
      - CodeSystem/asq-screening-result
      - CodeSystem/bssa-disposition
      - CodeSystem/bssa-item
      - CodeSystem/bssa-panel
      - CodeSystem/bssa-symptom
      - CodeSystem/cams-careplan-section
      - CodeSystem/cams-disposition
      - CodeSystem/cams-driver-category
      - CodeSystem/cams-driver-type
      - CodeSystem/cams-ssf
      - CodeSystem/cams-ssf-overall-risk
      - CodeSystem/cssrs-controllability
      - CodeSystem/cssrs-deterrents
      - CodeSystem/cssrs-duration
      - CodeSystem/cssrs-frequency
      - CodeSystem/cssrs-interval-item
      - CodeSystem/cssrs-lethality
      - CodeSystem/cssrs-potential-lethality
      - CodeSystem/cssrs-reasons
      - CodeSystem/cssrs-risk-level
      - CodeSystem/pss3-answer
      - CodeSystem/pss3-attempt-recency
      - CodeSystem/pss3-item
      - CodeSystem/pss3-panel
      - CodeSystem/pss3-result
      - CodeSystem/safet-panel
      - CodeSystem/safet-protective-factor
      - CodeSystem/safet-risk-factor
      - CodeSystem/sbqr-q1
      - CodeSystem/sbqr-q2
      - CodeSystem/sbqr-q3
      - CodeSystem/sbqr-q4
      - ValueSet/asq-result
      - ValueSet/asq-result-positive
      - ValueSet/bssa-disposition-elevated
      - ValueSet/bssa-disposition-vs
      - ValueSet/bssa-symptom-vs
      - ValueSet/cams-disposition-vs
      - ValueSet/cams-driver-type-vs
      - ValueSet/cams-ssf-measure
      - ValueSet/cams-ssf-overall-risk
      - ValueSet/cams-stabilization-plan-section
      - ValueSet/cams-therapeutic-worksheet-section
      - ValueSet/cssrs-interval-item
      - ValueSet/cssrs-risk-level
      - ValueSet/pss3-attempt-recency-vs
      - ValueSet/pss3-result-positive
      - ValueSet/pss3-result-vs
      - ValueSet/safet-protective-factor-vs
      - ValueSet/safet-risk-factor-vs
      - ValueSet/yes-no
      - CarePlan/cams-stabilization-careplan-example
      - CarePlan/ExampleCAMSStabilizationPlan
      - CarePlan/ExampleCAMSTherapeuticWorksheet
      - Condition/ExampleCAMSSuicideDriver
      - Observation/ExampleASQResultAcutePositive
      - Observation/ExampleASQResultNonAcutePositive
      - Observation/ExampleBSSADispositionEmergency
      - Observation/ExampleBSSADispositionFurtherEval
      - Observation/ExampleCAMSOutcomeDispositionResolved
      - Observation/ExampleCAMSSSFPsychologicalPain
      - Observation/ExampleCSSRSFullModerateRisk
      - Observation/ExampleCSSRSPediatricLowRisk
      - Observation/ExampleCSSRSScreenerHighRisk
      - Observation/ExampleCSSRSSinceLastContactModerateRisk
      - Observation/ExamplePHQ9Item9Positive
      - Observation/ExamplePHQ9TotalScore18
      - Observation/ExamplePSS3Positive
      - Observation/ExamplePSSFullModerateRisk
      - Observation/ExampleSAFETRiskLevelModerate
      - Observation/ExampleSBQRTotalScore9
      - QuestionnaireResponse/ExampleASQResponseNonAcute
      - QuestionnaireResponse/ExampleBSSAResponse
      - QuestionnaireResponse/ExampleCAMSOutcomeDispositionResponse
      - QuestionnaireResponse/ExampleCSSRSPediatricResponse
      - QuestionnaireResponse/ExampleCSSRSScreenerResponse
      - QuestionnaireResponse/ExampleCSSRSSinceLastContactResponse
      - QuestionnaireResponse/ExamplePHQ9Response
      - QuestionnaireResponse/ExamplePSS3ResponsePositive
      - QuestionnaireResponse/ExamplePSSFullResponse
      - QuestionnaireResponse/ExampleSAFETResponse
      - QuestionnaireResponse/ExampleSBQRResponse
  concept-layer:
    name: "Suicide-risk concept layer (Translate)"
    description: "The instrument-agnostic risk tier every tool maps into — the concept Observation, its tier and domain vocabularies, and the ConceptMaps and StructureMaps that derive it."
    resources:
      - StructureDefinition/coding-verification-status
      - StructureDefinition/spier-suicide-risk-concept
      - StructureDefinition/tier-derivation
      - ConceptMap/ASQDispositionToRiskTier
      - ConceptMap/BSSADispositionToRiskTier
      - ConceptMap/CAMSOverallRiskToRiskTier
      - ConceptMap/CSSRSRiskLevelToRiskTier
      - ConceptMap/PSS3ResultToRiskTier
      - ConceptMap/SPiERRiskTierToLOINC
      - CodeSystem/spier-coding-verification-status
      - CodeSystem/spier-concept-domain
      - CodeSystem/spier-suicide-risk-tier
      - CodeSystem/spier-tier-derivation
      - ValueSet/spier-coding-verification-status-vs
      - ValueSet/spier-concept-domain-vs
      - ValueSet/spier-suicide-risk-tier-vs
      - ValueSet/spier-tier-derivation-vs
      - Observation/ExampleSuicideRiskConceptFromASQ
      - Observation/ExampleSuicideRiskConceptFromCSSRS
      - Observation/ExampleSuicideRiskConceptFromPHQ9
      - Observation/ExampleSuicideRiskConceptFromSBQR
  safety-planning:
    name: "Safety planning and means safety"
    description: "The Stanley-Brown and Crisis Response Plan CarePlans, their shared section vocabulary, lethal-means counseling and crisis-resource sharing."
    resources:
      - Questionnaire/CrisisResponsePlan
      - Questionnaire/StanleyBrownSafetyPlan
      - ActivityDefinition/AdministerStanleyBrown
      - ActivityDefinition/AuthorCrisisResponsePlan
      - ActivityDefinition/ProvideMeansSafetyCounseling
      - ActivityDefinition/ShareCrisisResources
      - StructureDefinition/crisis-resource-code
      - StructureDefinition/spier-crisis-resources-shared
      - StructureDefinition/spier-crisis-response-plan
      - StructureDefinition/spier-lethal-means-counseling
      - StructureDefinition/spier-means-safety-action
      - StructureDefinition/spier-stanley-brown-safety-plan
      - CodeSystem/safety-plan-section
      - CodeSystem/spier-crisis-resource
      - CodeSystem/spier-lethal-means-method
      - CodeSystem/spier-means-safety-action
      - ValueSet/crisis-response-plan-section
      - ValueSet/spier-crisis-resource-vs
      - ValueSet/spier-lethal-means-method-vs
      - ValueSet/spier-means-safety-action-vs
      - ValueSet/stanley-brown-safety-plan-section
      - CarePlan/ExampleCrisisResponsePlan
      - CarePlan/ExampleStanleyBrownSafetyPlan
      - CarePlan/stanley-brown-safety-plan-hybrid-example
      - Communication/ExampleCrisisResourcesShared
      - Observation/ExampleMeansSafetyActionFirearm
      - Observation/ExampleMeansSafetyActionMedication
      - Procedure/ExampleLethalMeansCounseling
  handoffs-follow-up:
    name: "Handoffs and follow-up"
    description: "Transitions of care and what happens after them: the handoff, the discharge safety packet, referral, follow-up appointment, sharing consent, outreach attempts and caring contacts."
    resources:
      - ActivityDefinition/EscalateFollowUp
      - ActivityDefinition/FollowUpMissedAppointment
      - ActivityDefinition/GenerateDischargeSafetyPacket
      - ActivityDefinition/RecordConsentSharingStatus
      - ActivityDefinition/RecordFollowUpOutreach
      - ActivityDefinition/RecordTransitionCheckpoint
      - ActivityDefinition/ScheduleFollowUpAppointment
      - ActivityDefinition/SendCaringContact
      - ActivityDefinition/SendRapidReferral
      - ActivityDefinition/TrackFollowUpAppointment
      - StructureDefinition/caring-contact-opt-out
      - StructureDefinition/handoff-content-item
      - StructureDefinition/handoff-withheld-item
      - StructureDefinition/outreach-outcome
      - StructureDefinition/outreach-prompt
      - StructureDefinition/safety-concern-identified
      - StructureDefinition/spier-caring-contact
      - StructureDefinition/spier-discharge-safety-packet
      - StructureDefinition/spier-follow-up-appointment
      - StructureDefinition/spier-information-sharing-consent
      - StructureDefinition/spier-outreach-attempt
      - StructureDefinition/spier-safety-handoff
      - StructureDefinition/spier-safety-referral
      - CodeSystem/spier-consent-category
      - CodeSystem/spier-handoff-content
      - CodeSystem/spier-outreach-outcome
      - CodeSystem/spier-outreach-prompt
      - CodeSystem/spier-referral-reason
      - CodeSystem/spier-withholding-basis
      - ValueSet/spier-consent-category-vs
      - ValueSet/spier-handoff-content-vs
      - ValueSet/spier-outreach-outcome-vs
      - ValueSet/spier-outreach-prompt-vs
      - ValueSet/spier-referral-reason-vs
      - ValueSet/spier-withholding-basis-vs
      - Appointment/ExampleFollowUpAppointment
      - Communication/ExampleCaringContact
      - Communication/ExampleOutreachAttemptNoShow
      - Communication/ExampleOutreachAttemptReached
      - Communication/ExampleSafetyHandoff
      - Consent/ExampleInformationSharingConsent
      - DocumentReference/ExampleDischargeSafetyPacket
      - ServiceRequest/ExampleSafetyReferral
  risk-episode:
    name: "Risk episode and registry"
    description: "The suicide-safer care episode, its encounters, risk flag, open safety tasks, the reassessment schedule, and the verified problem-list vocabulary for clinician-asserted findings."
    resources:
      - ActivityDefinition/EscalateOverdueRisk
      - ActivityDefinition/MaintainRiskRegistry
      - ActivityDefinition/ScheduleRiskReassessment
      - ActivityDefinition/TrackOpenSafetyActions
      - ActivityDefinition/TrackRiskEpisodeStatus
      - PlanDefinition/SPiERReassessmentSchedule
      - StructureDefinition/episode-closure-reason
      - StructureDefinition/episode-current-risk-tier
      - StructureDefinition/episode-entry-reason
      - StructureDefinition/episode-trigger
      - StructureDefinition/escalation-trigger
      - StructureDefinition/spier-encounter
      - StructureDefinition/spier-safety-task
      - StructureDefinition/spier-suicide-related-condition
      - StructureDefinition/spier-suicide-risk-episode
      - StructureDefinition/spier-suicide-risk-flag
      - CodeSystem/spier-episode-closure-reason
      - CodeSystem/spier-episode-entry-reason
      - CodeSystem/spier-episode-type
      - CodeSystem/spier-escalation-trigger
      - CodeSystem/spier-risk-flag
      - CodeSystem/spier-safety-task-type
      - ValueSet/spier-episode-closure-reason-vs
      - ValueSet/spier-episode-entry-reason-vs
      - ValueSet/spier-episode-type-vs
      - ValueSet/spier-escalation-trigger-vs
      - ValueSet/spier-risk-flag-vs
      - ValueSet/spier-safety-task-type-vs
      - ValueSet/spier-suicide-related-problem-vs
      - Condition/ExamplePreviousSuicideAttemptProblem
      - Condition/ExampleSuicidalIdeationProblem
      - Encounter/ExampleEdEncounter
      - Encounter/ExampleFollowUpEncounter
      - EpisodeOfCare/ExampleActiveSuicideRiskEpisode
      - EpisodeOfCare/ExampleClosedSuicideRiskEpisode
      - Flag/ExampleSuicideRiskFlag
      - Task/ExampleCareGapTask
      - Task/ExampleEscalationTask
      - Task/ExampleReassessmentDueTask
  pathway:
    name: "The care pathway (Act)"
    description: "The eight stage PlanDefinitions and the Suicide Safer Care Pathway drawn from them, the placeholder steps, and the tool-id, licensing and coding-verification metadata every ActivityDefinition carries."
    resources:
      - ActivityDefinition/AdministerCARSS
      - ActivityDefinition/AdministerLocalRiskAssessment
      - ActivityDefinition/TriggerSuicideRiskWorkflow
      - PlanDefinition/SPiERClarifyRiskStage
      - PlanDefinition/SPiERCoordinateHandoffsStage
      - PlanDefinition/SPiERDefineRiskPictureStage
      - PlanDefinition/SPiERDocumentSafetyActionsStage
      - PlanDefinition/SPiERIdentifyPossibleRiskStage
      - PlanDefinition/SPiERMeasureAndShareStage
      - PlanDefinition/SPiERSuicideSaferCarePathway
      - PlanDefinition/SPiERTrackFollowUpStage
      - PlanDefinition/SPiERTrackRiskOverTimeStage
      - StructureDefinition/instrument-licensing-status
      - CodeSystem/spier-instrument-licensing-status
      - CodeSystem/spier-pathway-stage
      - ValueSet/spier-instrument-licensing-status-vs
      - NamingSystem/SPiERToolIdNamingSystem
  measures:
    name: "Measures (Stage 8)"
    description: "The quality measures scored over the artifacts above, the CQL Library they share, example MeasureReports, and the reporting, export and sharing activities."
    resources:
      - ActivityDefinition/ExportSuicideSaferCareData
      - ActivityDefinition/ProvideReportingDashboard
      - ActivityDefinition/ReportSuicideSaferCareMeasures
      - ActivityDefinition/ShareSuicideSaferCareData
      - Measure/SPiERCaringContactAdherence
      - Measure/SPiERFollowUpTimeliness
      - Measure/SPiERLethalMeansCounselingCompleted
      - Measure/SPiERReassessmentOnTime
      - Measure/SPiERReferralCompletion
      - Measure/SPiERRiskStatusDocumented
      - Measure/SPiERSafetyPlanBeforeDischarge
      - Measure/SPiERScreenToAssessment
      - Library/SPiERSuicideSaferCareMeasures
      - CodeSystem/spier-measure-group
      - ValueSet/spier-measure-group-vs
      - Appointment/ExampleMeasuredFollowUpAppointment
      - Communication/ExampleMeasuredOutreachAttempt
      - MeasureReport/ExampleCaringContactAdherenceReportSummary
      - MeasureReport/ExampleFollowUpTimelinessReportIndividual
      - MeasureReport/ExampleFollowUpTimelinessReportSummary
  conformance:
    name: "Conformance"
    description: "The CapabilityStatement for each system role this guide defines."
    resources:
      - CapabilityStatement/hie-intermediary
      - CapabilityStatement/quality-reporter
      - CapabilityStatement/risk-consumer
      - CapabilityStatement/screening-source-ehr
resources:
  StructureMap/ASQResultToSuicideRiskConcept:
    name: "ASQResultToSuicideRiskConcept"
    groupingId: concept-layer
  StructureMap/CSSRSRiskLevelToSuicideRiskConcept:
    name: "CSSRSRiskLevelToSuicideRiskConcept"
    groupingId: concept-layer
  StructureMap/PHQ9Item9ToSuicideRiskConcept:
    name: "PHQ9Item9ToSuicideRiskConcept"
    groupingId: concept-layer
  StructureMap/SBQRTotalScoreToSuicideRiskConcept:
    name: "SBQRTotalScoreToSuicideRiskConcept"
    groupingId: concept-layer
  StructureMap/StanleyBrownQRToCarePlan:
    name: "StanleyBrownQRToCarePlan"
    groupingId: safety-planning
# ── END generated groups ──

# Pages and menu structure
pages:
  index.md:
    title: Home
  getting-started.md:
    title: Getting Started
  how-to-read.md:
    title: Reading the artifacts
  relationship-to-other-igs.md:
    title: Relationship to Other IGs
  conformance.md:
    title: Conformance
  quick-starts.md:
    title: Quick Starts
  zero-suicide-mapping.md:
    title: Zero Suicide ↔ SPiER mapping
  care-pathway.md:
    title: Care Pathway
  measurement.md:
    title: Measures
  design-decisions.md:
    title: Design decisions

# Information architecture mirrors HL7 US Core (Home · Getting Started ·
# Guidance · Conformance · Quick Starts · Artifacts) so adopters meet a familiar
# layout. `artifacts.html` is generated by the IG Publisher; the page itself is
# not in `pagecontent/`, which is why `check-ig-menu.mjs` allowlists it.
#
# US Core also carries a Downloads entry and this IG deliberately does not:
# `fhir.base.template#current` emits no `downloads.html` (that is a US Core
# template convention), so declaring one here would render a broken link. The
# package is still published — `package.tgz` and `full-ig.zip` sit at the IG
# root — and `getting-started.md` is where readers are pointed at it.
#
# `how-to-read.md` used to restate this block in prose ("## The menu"), gated by
# `scripts/check-ig-menu.mjs` checks A/B (#409). The restatement went in the IG
# cleanup (2026-09-16): the navigation bar is the menu, Home's "Find what you
# need" table sends readers by task rather than by menu entry, and every link
# in it is checked by check-ig-narrative's check H. Checks C and D below remain.
#
# ⚠️ A page needs BOTH blocks. `menu:` makes it reachable; `pages:` above is
# what makes the publisher render it. A menu entry whose page is missing from
# `pages:` is not one broken link — the menu is on every rendered page, so it is
# one per page: adding `care-pathway.html` to `menu:` alone took the IG from 0 to
# **1812** broken links, with 0 errors and a clean SUSHI run. `check-ig-menu.mjs`
# check D now compares the two blocks in both directions.
menu:
  Home: index.html
  Getting Started: getting-started.html
  Guidance:
    Reading the artifacts: how-to-read.html
    Relationship to Other IGs: relationship-to-other-igs.html
    Zero Suicide ↔ SPiER mapping: zero-suicide-mapping.html
    Care Pathway: care-pathway.html
    Measures: measurement.html
    Design decisions: design-decisions.html
  Conformance: conformance.html
  Quick Starts: quick-starts.html
  Artifacts: artifacts.html
