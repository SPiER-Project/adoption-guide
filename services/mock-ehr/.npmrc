# See the root .npmrc and package.json's "engines" field — this repo has no
# npm workspaces (#387), so each service needs its own copy for `npm install`
# run from inside this directory to actually enforce it.
engine-strict=true
