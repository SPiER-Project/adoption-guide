{
  "name": "@spier/cds-hooks",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "SPiER on Cloudflare Workers — one Worker serving the adoption-guide SPA (Static Assets) and the CDS Hooks 2.0 API, sharing the app's browser-free derivation code.",
  "scripts": {
    "build:web": "npm --prefix ../../web run build",
    "stage:assets": "rm -rf web-dist && cp -R ../../web/dist ./web-dist",
    "build:worker": "vite build",
    "build": "npm run build:web && npm run stage:assets && npm run build:worker",
    "dev": "npm run build:web && npm run stage:assets && npm run build:worker && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "pretest": "npm --prefix ../../web run copy-fhir",
    "test": "vitest run",
    "verify": "npm --prefix ../../web run copy-fhir && npm run typecheck && npm run lint && npm run test"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "jose": "^6.2.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "@types/node": "^22.20.1",
    "@eslint/js": "^10.0.1",
    "@stylistic/eslint-plugin": "^5.10.0",
    "eslint": "^10.8.0",
    "globals": "^17.8.0",
    "typescript": "~5.9.3",
    "typescript-eslint": "^8.65.0",
    "vite": "^7.3.1",
    "vitest": "^3.0.5",
    "wrangler": "^4.0.0"
  }
}
